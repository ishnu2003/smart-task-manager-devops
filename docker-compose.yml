services:
  task-manager-staging:
    build: .
    image: smart-task-manager:staging
    container_name: task-manager-staging
    ports:
      - "5051:5000"
    environment:
      - FLASK_ENV=staging
      